from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Predict the propensity of a customer to churn',
    author='Nikit',
    license='MIT',
)
